name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  bloc:
  flutter_bloc:
  flutter:
    sdk: flutter

dependency_overrides:
  meta: ^1.14.0

dev_dependencies:
  custom_lint:
  pedant:
    path: ../

flutter:
  uses-material-design: true