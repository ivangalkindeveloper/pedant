name: example
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.3.3 <4.0.0'

dependencies:
  bloc:
  flutter:
    sdk: flutter
  flutter_bloc:

dev_dependencies:
  custom_lint:
  pedant:
    path: ../

dependency_overrides:
  meta: ^1.14.0

flutter:
  uses-material-design: true