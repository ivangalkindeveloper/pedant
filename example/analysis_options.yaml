analyzer:
  plugins:
    - custom_lint

custom_lint:
  debug: true
  verbose: true
  rules:
    - pedant:
      add_class_postfix_by_keyword_list:
        -
          keywordList:
            - base
          name: Base
      add_class_postfix_by_path_list:
        -
          path: lib/add/add_class_postfix_by_path_list
          nameList: 
            - AppBar
            - Button
      add_class_prefix_by_keyword_list:
        -
          keywordList:
            - abstract
            - interface
            - sealed
          name: I
      add_class_prefix_by_path_list:
        -
          path: lib/add/add_class_prefix_by_path_list
          nameList: 
            - FF
      edit_file_length_by_path_list:
        - 
          path: lib/edit/edit_file_length_by_path_list
          length: 200